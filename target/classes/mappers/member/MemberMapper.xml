<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Maptter 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<!-- 다른 mapper와 중복되지 않도록 네임 스페이스 기재 -->
<mapper namespace="member">
	
	<insert id="insertMember">
		insert into member(memberMail, memberPassword, memberName, memberMobile)
		values (#{memberEmail}, #{memberPassword}, #{memberName}, #{memberMobile})
	</insert>
	
	<select id="loginMember" resultType="java.lang.Boolean">
		select decode(count(*),1,'true', 'false')
		from member
		where memberMail= #{memberEmail} and memberPassword = #{memberPassword}
	</select>
</mapper>